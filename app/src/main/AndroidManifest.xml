<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sharelearnproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".shejimoshi.agent.AgentActivity"></activity>
        <activity android:name=".annotations_fanshe.AnnotationIntentActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".annotations_fanshe.AnnotationTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- 避免警告 -->
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>